#include "main.h"
/**
 * _strstr - Entry point
 *@haystack: input
 *@needle: input
 * Return: always 0 (Success)
 */
char *_strstr(char *haystack, char *needle)
{
	int k;

		for (k = 0; haystack[k]; k++)
		{
		if (k = 0;  haystack[k])
		return (0);
		}
	return (0);
}
